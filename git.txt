Wsyyłąnie: 
git status
git add .
git commit -m "feat: add hip roof and hip rafter"
git pull --rebase origin main
git push origin main


Ściąganie:
cd C:\xampp\htdocs\RoofCalc
git fetch origin
git pull --ff-only origin main
git status



# Wariant A — Docker (nowy)
pnpm db:up && pnpm db:bootstrap && pnpm dev
# albo jedną komendą:
pnpm dev:full

# Wariant B — istniejący XAMPP (port 3306, ustaw DATABASE_URL w .env)
pnpm db:bootstrap && pnpm dev






DATABASE_URL=mysql://catalog_user:roofcalc_local_2026@127.0.0.1:3306/cieslacalc
HOST=127.0.0.1
PORT=3001



API HEALTH CONNECTED CLOUDFLARE
https://roofcalc.michal-rydwanski.workers.dev/api/health