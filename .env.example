# Optional. Geometry and saved local projects work without the catalogue DB/API.
# For the default XAMPP MariaDB installation create the database first and adapt
# the user/password explicitly; never commit real credentials.
DATABASE_URL=mysql://catalog_user:change-me@127.0.0.1:3306/cieslacalc
HOST=127.0.0.1
PORT=3001
